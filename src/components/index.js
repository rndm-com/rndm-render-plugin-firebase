import Config from './Config';
import Element from './Element';

const components = [
  {
    type: 'Wrapper.Config',
    value: Config,
  },
  {
    type: 'Element',
    value: Element,
  },
];

export default components;
